const vec3 caveFogColor = vec3(0.0125, 0.03125, 0.05) * 1.0;

// Todo: should be configurable
const vec3 endMistColor = vec3(0.0, 0.15, 0.4) * 1.0;



const float WATER_DIRT_AMOUNT = 0.25;
const vec3 WATER_COLOR = vec3(0.0, 0.20, 0.25);